spring:
  cloud:
    kubernetes:
      reload:
        enabled: true
      config:
        enabled: true
        sources:
          - name: feature-flags
  config:
    activate:
      on-cloud-platform: kubernetes